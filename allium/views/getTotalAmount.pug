extends base
block content
    div.table
        h3 Кількість товару на складі:
        table(id="keywords" cellspacing="0" cellpadding="0")
            thead
                tr
                    th: span ID
                    th: span Товар
                    th: span Кількість
            tbody
                each res in result
                    tr
                        td(class="lalign")= res.id_product
                        td= res.name
                        td= res.amount